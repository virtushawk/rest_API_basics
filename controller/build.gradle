plugins {
    id 'java'
    id "org.springframework.boot" version "2.5.1"
}

dependencies {
    implementation project(':model')
    implementation project(':service')
}

bootRun {
    main = 'com.epam.esm.config.ApplicationConfiguration'
}